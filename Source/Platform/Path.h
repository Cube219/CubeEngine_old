#pragma once
// TODO: ³ªÁß¿¡
/*
#include "PlatformHeader.h"

#include "PlatformString.h"

namespace cube
{
	namespace platform
	{
		class PLATFORM_EXPORT Path
		{
		public:
			Path();
			Path(const Character* pathStr);
			Path(const String& pathStr);
			Path(const PCharacter* pathStr);
			Path(const PString& pathStr);

			void Assign(const PCharacter* pathStr);

			PString GetBaseName() const { return mBaseName; }
			PString GetDirectoryName();

			Path GetAbsolute() const;
			Path GetRelative() const;
			Path GetParent() const;
			
			void Absolute();
			void Relative();
			void Parent();

			bool IsExist() const;
			bool IsFile() const { return mIsFile; }
			bool IsDirectory() const { return mIsDirectory; }
			bool IsAbsolute() const { return mIsAbsolute; }

		private:
			Vector<PString> mDirectories;
			PString mBaseName;

			bool mIsFile;
			bool mIsDirectory;
			bool mIsAbsolute;
		};
	} // namespace platform
} // namespace cube
*/